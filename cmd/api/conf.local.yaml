database:    
  psn: host=localhost port=5432 user=postgres dbname=CrudTest password=mpc3000 sslmode=disable

server:
  port: :8080
  debug: true
  read_timeout_seconds: 10
  write_timeout_seconds: 5
